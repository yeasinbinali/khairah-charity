import React from 'react';
import Carousel from '../Carousel/Carousel';
import Causes from '../Causes/Causes';

const Home = () => {
    return (
        <div>
            <Carousel></Carousel>
            <Causes></Causes>
        </div>
    );
};

export default Home;